<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.osolapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".description"></activity>
        <activity android:name=".connect" />
        <activity android:name=".DataView" />
        <activity android:name=".Recherche" />
        <activity
            android:name=".creation_compte"
            android:label="@string/title_activity_creation_compte"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".info_pico_osol" />
        <activity android:name=".info_pico_user" />
        <activity android:name=".connection_user" />
        <activity android:name=".connection_osol" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>